#!/bin/bash

# Скрипт для встановлення правильних прав доступу для Odoo

# Отримуємо директорію скрипта
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
PROJECT_DIR="$(cd "${SCRIPT_DIR}/.." && pwd)"

# Переходимо в директорію проекту
cd "${PROJECT_DIR}"

echo "🔧 Встановлення прав доступу для Odoo..."
echo ""

# Odoo контейнер працює під користувачем odoo (UID 101)
# Встановлюємо права так, щоб всі могли читати, але тільки власник може писати
# Це дозволить Odoo читати файли незалежно від UID на хості

# Папки, які потрібно налаштувати
DIRS=(
    "addons"
    "config"
    "logs"
)

# Встановлюємо права для папок
for dir in "${DIRS[@]}"; do
    if [ -d "${dir}" ]; then
        echo "📁 Налаштування прав для ${dir}/..."
        # Встановлюємо права 755 для папок (rwxr-xr-x)
        find "${dir}" -type d -exec chmod 755 {} \;
        # Встановлюємо права 644 для файлів (rw-r--r--)
        find "${dir}" -type f -exec chmod 644 {} \;
        # Встановлюємо права виконання для .sh файлів
        find "${dir}" -type f -name "*.sh" -exec chmod 755 {} \;
        echo "   ✅ Готово"
    else
        echo "⚠️  Папка ${dir}/ не існує, пропускаю..."
    fi
done

# Спеціальні права для Python файлів
if [ -d "addons" ]; then
    echo ""
    echo "🐍 Налаштування прав для Python файлів..."
    find addons -type f -name "*.py" -exec chmod 644 {} \;
    find addons -type f -name "__init__.py" -exec chmod 644 {} \;
    echo "   ✅ Готово"
fi

# Створюємо папки, якщо їх немає
mkdir -p logs
mkdir -p backups
mkdir -p output

# Встановлюємо права для нових папок
chmod 777 logs 2>/dev/null || true  # 777 для logs, щоб Odoo міг писати
chmod 755 backups output 2>/dev/null || true

# Створюємо файл логу, якщо його немає, з правильними правами
if [ ! -f "logs/odoo.log" ]; then
    touch logs/odoo.log
    chmod 666 logs/odoo.log  # rw-rw-rw- щоб Odoo міг писати
    echo "📝 Створено файл логу: logs/odoo.log"
fi

echo ""
echo "✅ Права доступу встановлено!"
echo ""
echo "💡 Якщо Odoo все ще не бачить модулі, перевірте:"
echo "   1. Чи правильно вказаний шлях в config/odoo.conf"
echo "   2. Чи всі модулі мають правильну структуру (__init__.py, __manifest__.py)"
echo "   3. Перезапустіть контейнери: docker compose restart"
