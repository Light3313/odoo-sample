# Утиліти для роботи з Odoo

Ця папка містить корисні скрипти для управління та роботи з Odoo.

## 📋 Доступні скрипти

### 🔄 Робота з базою даних

#### `backup-db.sh` - Створення резервної копії
Створює резервну копію бази даних PostgreSQL.

```bash
./utils/backup-db.sh [назва_бази_даних]
```

Якщо назва БД не вказана, скрипт покаже список доступних БД і попросить вибрати.

```bash
# З вибором БД інтерактивно
./utils/backup-db.sh

# З прямою вказівкою БД
./utils/backup-db.sh my_odoo_db
```

Резервні копії зберігаються в папці `backups/` з іменем:
`odoo_backup_<db_name>_<timestamp>.sql.gz`

#### `restore-db.sh` - Відновлення з резервної копії
Відновлює базу даних з резервної копії.

```bash
./utils/restore-db.sh <backup_file> [назва_бази_даних]
```

Якщо назва БД не вказана, скрипт покаже список доступних БД або попросить ввести назву для нової БД.

```bash
# З вибором БД інтерактивно
./utils/restore-db.sh backups/odoo_backup_postgres_20250116_120000.sql.gz

# З прямою вказівкою БД
./utils/restore-db.sh backups/odoo_backup_postgres_20250116_120000.sql.gz my_odoo_db
```

**Увага:** Ця операція перезапише поточну базу даних! Якщо БД не існує, вона буде створена автоматично.

#### `reset-db.sh` - Скидання бази даних
Видаляє всі дані з бази даних та створює нову порожню БД.

```bash
./utils/reset-db.sh [назва_бази_даних]
```

Якщо назва БД не вказана, скрипт покаже список доступних БД і попросить вибрати.

```bash
# З вибором БД інтерактивно
./utils/reset-db.sh

# З прямою вказівкою БД
./utils/reset-db.sh my_odoo_db
```

**Увага:** Це операція НЕЗВОРОТНА! Використовуйте тільки для розробки.

#### `clear-data.sh` - Очищення даних (видалення записів)
Видаляє тільки дані (контакти, товари, продажі тощо), залишаючи структуру таблиць та модулі.

```bash
./utils/clear-data.sh [назва_бази_даних]
```

**Що видаляється:**
- Контакти (res.partner)
- Товари (product.product, product.template)
- Продажі (sale.order, sale.order.line)
- Закупівлі (purchase.order)
- Склад (stock.picking, stock.move)
- Рахунки (account.move, account.payment)
- Інші бізнес-дані

**Що залишається:**
- Структура таблиць
- Модулі та їх налаштування
- Системні записи (ir.*)
- Конфігурація Odoo

```bash
# З вибором БД інтерактивно
./utils/clear-data.sh

# З прямою вказівкою БД
./utils/clear-data.sh my_odoo_db
```

**Увага:** Це операція НЕЗВОРОТНА! Всі бізнес-дані будуть видалені, але модулі залишаться.

### 🔍 Інформація та моніторинг

#### `show-info.sh` - Інформація про систему
Виводить детальну інформацію про систему, версії, статуси контейнерів.

```bash
./utils/show-info.sh
```

Показує:
- Статус Docker контейнерів
- Версії Docker, Odoo, PostgreSQL
- Порти та URL
- Інформацію про базу даних
- Розміри папок та кількість резервних копій

#### `list-modules.sh` - Список модулів
Виводить повний список всіх модулів Odoo з їх статусами.

```bash
./utils/list-modules.sh
```

Показує:
- ✅ Встановлені модулі
- ❌ Не встановлені модулі
- 📥 Модулі до встановлення
- ⬆️ Модулі до оновлення
- **Залежності модулів** (від яких модулів залежить кожен модуль)
- Статистику по модулях

Для кожного модуля виводиться список модулів, від яких він залежить (через кому). Якщо залежностей немає, показується "немає".

Результат зберігається в `output/modules_list_<db_name>_<timestamp>.txt`

### 🐚 Робота з контейнерами

#### `shell-web.sh` - Вхід в контейнер Odoo
Швидкий вхід в контейнер Odoo для виконання команд.

```bash
./utils/shell-web.sh
```

#### `shell-db.sh` - Вхід в контейнер PostgreSQL
Вхід в PostgreSQL контейнер з автоматичним підключенням до БД.

```bash
./utils/shell-db.sh
```

Корисні команди в psql:
- `\l` - Список баз даних
- `\dt` - Список таблиць
- `\q` - Вийти

#### `run-odoo-command.sh` - Виконання Odoo CLI команд
Виконує команди Odoo через CLI.

```bash
./utils/run-odoo-command.sh -d mydb -u base --stop-after-init
./utils/run-odoo-command.sh -d mydb shell
```

#### `install-module.sh` - Встановлення та оновлення модулів
Встановлює та оновлює модулі Odoo через CLI.

```bash
./utils/install-module.sh <modules> [options]
```

**Параметри:**
- `<modules>` - Модулі для встановлення (через кому, наприклад: `o1c_cron,base`)
- `-d, --database <name>` - Назва бази даних (опціонально, буде вибір якщо не вказано)
- `-u, --update <modules>` - Модулі для оновлення перед встановленням (через кому)
- `--no-stop` - Не зупиняти після встановлення (за замовчуванням зупиняє)

**Приклади:**
```bash
# Встановити модуль з автоматичним вибором БД
./utils/install-module.sh o1c_cron

# Встановити модуль з вказівкою БД
./utils/install-module.sh o1c_cron -d my_odoo_db

# Встановити модуль з оновленням залежностей
./utils/install-module.sh o1c_cron -u o1c,o1c_import,base_automation

# Встановити модуль з оновленням та без зупинки
./utils/install-module.sh o1c_cron -d my_odoo_db -u o1c,o1c_import --no-stop
```

**Примітка:** За замовчуванням скрипт зупиняє Odoo після встановлення (`--stop-after-init`). Використайте `--no-stop` якщо потрібно залишити Odoo запущеним.

## 📁 Структура результатів

- **`backups/`** - Резервні копії баз даних (`.sql.gz` файли)
- **`output/`** - Результати роботи скриптів (списки модулів, звіти тощо)

## 💡 Приклади використання

### Щоденна резервна копія
```bash
# Додайте в crontab для автоматичного backup
0 2 * * * cd /path/to/odoo-sample && ./utils/backup-db.sh
```

### Перевірка статусу системи
```bash
./utils/show-info.sh
```

### Встановлення модулів через CLI
```bash
# Встановити модуль
./utils/install-module.sh o1c_cron -d mydb

# Встановити модуль з оновленням залежностей
./utils/install-module.sh o1c_cron -d mydb -u o1c,o1c_import,base_automation
```

### Оновлення модулів через CLI
```bash
./utils/run-odoo-command.sh -d mydb -u all --stop-after-init
```

### Перевірка встановлених модулів
```bash
./utils/list-modules.sh | grep "✅ Встановлені"
```

### Права доступу

#### `fix-permissions.sh` - Встановлення прав доступу
Встановлює правильні права доступу для папок addons, config, logs.

```bash
./utils/fix-permissions.sh
```

Цей скрипт автоматично викликається при запуску `quick-start.sh`.

**Коли використовувати:**
- Після копіювання нових модулів в папку addons
- Якщо Odoo не бачить модулі
- Після зміни файлів вручну

## ⚠️ Важливо

- Всі скрипти автоматично завантажують налаштування з `.env` файлу
- Переконайтеся, що контейнери запущені перед використанням скриптів
- Резервні копії та результати зберігаються локально в папках `backups/` та `output/`
- Якщо Odoo не бачить модулі, запустіть `./utils/fix-permissions.sh`